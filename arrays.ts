const superHeros: string[] = [] 
// alternative: 
const superPower: Array<number> = [] 

type User = {
    name: string,
    isActive: boolean
}

const allUsers: User[] = []

// tensor-flow, numpy data:

const MLModels: number[][] = [
    [255, 255, 255], 
    [23, 23, 23]
]

superHeros.push("spider")
superPower.push(2)
allUsers.push({name: "test", isActive: true})

export {}

